This readme file outlines the steps needed to take to run the pipeline...

Preliminary Step:
- Download OBSIDS
- Unzip relavent files


Rerun only the canonical center step:

```bash
python /home/carterrhea/Documents/Lemur/Pipeline/pipeline.py --recompute-centers
```

Rerun only the double-beta fit step:

```bash
python /home/carterrhea/Documents/Lemur/Pipeline/pipeline.py --recompute-double-beta
```
